# Seed avatars [Owner: A / D]

Drop 30 placeholder avatar images here (e.g. from a free avatar generator)
named to match the seed script's student ids, and reference them from
`prisma/seed.ts`'s `avatarUrl` field as `/avatars/<file>`.
