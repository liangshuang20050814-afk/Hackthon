# [Owner: A] SQLite file used by Prisma. No setup needed, just keep this path.
DATABASE_URL="file:./dev.db"

# [Owner: A] Optional Python matching service for /api/matches.
# Leave unset to use the local TypeScript fallback scorer.
MATCHING_SERVICE_URL=
